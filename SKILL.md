---
name: proposal-creator
description: 写品牌传播方案（比稿 / 提案），产出逐页内容加可编辑的 PPT、讲稿和换图清单。当用户要写方案、写提案、写比稿、写传播方案、写 campaign 方案，或丢来一个客户 brief 说「帮我出个方案」时使用。也适用于「把这些内容做成 PPT」「给这个方案配图」「写个讲稿」。不适用于单篇文案、新闻稿、活动执行手册——那些不是提案。
---

# 写方案

给品牌客户写传播方案。产出不是一份文档，是**一套交付物**：
逐页内容 + 可编辑的 PPT + 讲稿 + 换图清单。

MIT 协议，作者 vinci，版本 1.0.0，工作语言中文。

**这个 skill 需要的能力**：读写文件、执行 shell 命令、联网搜索。
下面很多步骤要跑 Python 脚本，没有 shell 权限就退化成手工操作。

**环境要求**：Python 3，以及 `python-pptx`、`Pillow`、`numpy`。
背景合成和 PPT 生成跨平台可用。姓名卡要往图上画中文，用到 macOS 的
字体文件，其他平台会自动退回默认字体——不崩，但中文会变方块。
素材上传（可选的那一步）需要 Chrome 或 Edge；不做也不影响出 PPT。

## 先定位脚本目录

下面会多次用到本 skill 的 `scripts/`。**Claude Code 和 Codex 读的目录不同**，
所以别写死路径，第一次要用之前先定位一次：

```bash
SKILL=$(ls -d ~/.claude/skills/proposal-creator \
               ~/.agents/skills/proposal-creator 2>/dev/null | head -1)
```

（用软链接装的，两个路径都在，取到哪个都对。装在别处就自己指过去。）
下文一律用 `$SKILL` 加相对路径指代脚本。

## 开场第一件事：亮流程

**skill 一启动，先把完整流程打印出来，并标出当前在哪一步。**
每完成一个节点再重印一次，让用户随时知道走到哪儿了。

```
① 提纲 / storyline ──→ 【卡点1 · 审逻辑】
② 每页具体内容    ──→ 【卡点2 · 审内容】
③ 数据与预算核对  ──→ 【卡点3 · 核数字】
④ 风格与素材 → 样稿 → 全案 PPT
⑤ 讲稿 + 换图清单
```

三个卡点是**硬关卡**：不确认不往下走。用户改完再重印流程。

---

## 所有提问都走 HTML，不走对话框

要用户拍板的每一件事，都做成一份**能勾选、能填、能传图、能一键导出**的 HTML。
对话框里只留一句「本批 N 个问题在 `<文件名>.html` 底部」。

做法见 `references/提问页规格.md`。一句话：**用户经常攒着一批问题一起处理**，
一问一答的往返成本他付不起；而且光把问题列出来不够，他看完还得回来打字，等于没省。

---

## 节点 ① 提纲 / storyline

先读客户 brief，再写提纲。**别急着填内容——这一关只审逻辑。**

提纲要能看出：解读需求 → 定传播目标 → 出策略 → 给创意 → 落执行 → 算预算。
模块按需求增减，但这条骨架不能断。

**每一章都要写清「这一章回答客户的哪个问题」**，答不上来的章节就是多余的。

产出 `01-提纲-storyline.html`。每页给出：页码、标题、这页要说什么、
**为什么这么排（逻辑理由）**、以及一个反馈位。

### 【卡点1】停下来等用户审逻辑。
逻辑不通，后面全白做。

---

## 节点 ② 每页具体内容

按提纲逐页写内容。这一步的产出**直接决定 PPT 长什么样**，所以要按页来想。

**一页只讲一件事。** 这是硬约束，不是建议——详见 `references/红线与边界.md` 第 7 条。
写完一页自问：这页如果只能留一句话，是哪句？那句之外的都该考虑下沉。

按章节拆成几个 HTML（`02-内容-xxx.html`），每页包含：页码、标题、正文文案、
这页的支撑点。

### 【卡点2】停下来等用户审内容。

---

## 节点 ③ 数据与预算核对清单

**方案里每一个数字，都要在这一关过一遍。** 这是四条红线里最要命的一条。

逐条列出：结论 / 来源 URL / 可靠性（高·中·低）/ 建议（可用·拿掉·要改）。
产出 `数据与预算核对清单.html`，让用户逐条打勾。

- 找不到公开来源的，**直接写「未找到公开来源」**，不许用推测填补。
- 预算必须用户审核。推算值要标明是推算值，并写清「需与客户确认拆分口径」。
- 页脚给出统计：`共 N 条，已核对 N 条，未标记 0 条。`

### 【卡点3】停下来等用户核数字。

---

## 节点 ④ 风格与素材 → 样稿 → 全案

这一段最容易返工，所以拆成三步。**四十几页出完再改风格等于全部重做。**

### 4.1 风格与素材

出 `风格与素材.html`：

- **背景方向 / 色彩体系 / 字体方案**，三组单选，**每组配可视化预览**。
  选风格这件事文字描述没有意义，必须看图。
- **逐页素材表**：页码 / 标题 / 这页要什么图 / 需求等级 / 上传槽 / 批注。
- **图非必须**，没有的页留占位框。

用户一次勾完。上传走 File System Access API 直接写进 `产出/素材/`，
**图片只落本机，不上传任何外部服务**。细节见 `references/提问页规格.md`。

### 4.2 建项目配置

在 `产出/` 下建 `_project.json`，把会变的东西全关进去：路径、强调色、
章节名与章节色、字体、品牌名。工具箱从这儿读，所以换方案不用改一行代码。

```json
{
  "base": "<产出目录>/",
  "accent": "C9A227",
  "brand": "品牌 wordmark", "sub_brand": "副标识", "client": "客户名",
  "fonts": {"cn_serif":"Songti SC","en_serif":"Georgia",
            "cn_sans":"PingFang SC","en_sans":"Helvetica Neue"},
  "sections": {
    "01": {"name":"项目理解","en":"Decoding the Brief","color":"5B6B7C"}
  },
  "cover_bg":"B-破晓.jpg","content_bg":"A-雾.jpg","quiet_bg":"D-静夜.jpg"
}
```

### 4.3 生成背景

背景**自己合成，不去网上找图**——合成的东西无版权、可商用、可无限复刻，
不存在「这张图从哪来的」这个问题。搬别人的素材是红线。

```bash
python3 "$SKILL/scripts/bg.py" <产出目录>
```

出一套背景家族（约 10 张，共 1.7 MB），**全案复用**。
python-pptx 按内容 SHA1 去重，复用不额外占体积。

### 4.4 出样稿

**4 页就够**，覆盖封面 / 章节页 / 内容页 / 主张页四个版面原型。
给用户看完再往下。这是最后一个便宜的返工点。

### 4.5 出全案

从 `assets/deck-template.py` 起手复制到 `产出/_src/deck.py`，照着往下加页。

```bash
cd <产出>/_src && python3 deck.py
```

**排完必须跑自检**，肉眼翻缩略图看不出溢出：

```bash
python3 "$SKILL/scripts/render.py" <产出>/全案-v1.pptx /tmp/r
```

它会逐页报「文字溢出」和「越界」。**修到 0 为止**——别把警告留给用户。

---

## 节点 ⑤ 讲稿 + 换图清单

### 讲稿（必出）

`讲稿.html`——逐页「这页放什么字」＋「这页怎么讲」两栏。

**PPT 上只留精简后的字，讲解要点、口头补充、数据出处、被砍掉的细节全落这里。**
注释是给用户对着客户讲的：这一页重点是什么、哪些不用念、客户可能问什么。

从 .pptx 直接读内容来生成，这样讲稿**永远不会和 PPT 脱节**。

### 换图清单（用了占位素材就必出）

说清每个位置换成什么、从哪来。**别抓明星和博主的头像**——
抓到了也没法确认那张脸对着那个名字，方案里出现「脸和名字对不上」
比留个空位置严重得多。做法见 `references/pptx-技术要点.md` 第十条。

---

## 红线

写每一句话、放每一个数字之前，先读 `references/红线与边界.md`。四条底线：

1. **不编造数据。** 所有数据必须有出处，不确定的标注出来交给用户。
2. **可参考竞品，不照抄创意。** 复刻架构，不复刻版面。
3. **预算必须用户审核。**
4. **不承诺效果。**

外加三条颗粒度边界：**不写执行细节**（写「谁」和「为什么是他」，
不写「他到场做什么」）、**不主动写风险**、**一页只讲一件事**。

文案本身受 `references/语言规则.md` 约束——**那份规则不准出现在方案里**，
它是约束不是内容。判断标准：客户会不会为这条信息付钱？

---

## 交付时要说清的两件事

- **预览是我自己写的渲染器画的，不是 PowerPoint 渲染的。** 它能验文件结构
  （越界 / 溢出 / 缺字），最终效果得用户打开看。不要让用户以为预览就是成品。
- **占位符不许编。** 团队配置、相关案例、联系方式这类只有用户有的信息，
  留「待你填」。方案上一个空位置，比一个编错的名字轻得多。

---

## 配套文件

| 文件 | 什么时候读 |
|---|---|
| `references/红线与边界.md` | **写之前必读**：四条红线 + 颗粒度边界 |
| `references/语言规则.md` | 写每一句文案时的约束，不进方案正文 |
| `references/提问页规格.md` | 做提问 HTML / 素材上传页时 |
| `references/pptx-技术要点.md` | 改工具箱、或遇到排版诡异现象时 |
| `scripts/kit.py` | 版面工具箱，全部原语。**先读顶部注释再用** |
| `scripts/cfg.py` | 读 `_project.json`，换方案只改 json |
| `scripts/bg.py` | 合成背景家族 |
| `scripts/render.py` | 自检渲染器，**排完必跑** |
| `scripts/unpack.py` | 解开用户导出的 `素材包.json` |
| `assets/deck-template.py` | 建 deck 的骨架，每个原语演示一次 |

## 这个 skill 怎么改

| 症状 | 改哪里 |
|---|---|
| 该触发时没触发 | 改 description，补触发词 |
| 不该触发时乱触发 | 改 description，补「不适用于」 |
| 每次结果不一样 | 流程有歧义，去正文把那一步写具体 |
| 排版老出问题 | 改 `scripts/kit.py`，别在 deck 脚本里绕过去 |
| 正文越改越长 | 加新内容前先删等量的旧内容——每条都会在每次触发时消耗上下文 |
